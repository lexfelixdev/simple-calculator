spring:
  datasource:
    url: jdbc:mysql://localhost:3306/calculationserver
    username: calcserver
    password: calcserver
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update